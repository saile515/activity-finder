{
	"name": "backend",
	"version": "1.0.0",
	"description": "",
	"main": "dist/bundle.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "webpack --mode production",
		"dev": "npm-run-all --parallel dev:build dev:run",
		"dev:build": "webpack --watch --mode development",
		"dev:run": "nodemon --inspect"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"ts-loader": "^9.3.1",
		"typescript": "^4.7.4",
		"webpack": "^5.74.0",
		"webpack-cli": "^4.10.0"
	},
	"dependencies": {
		"npm-run-all": "^4.1.5"
	}
}
